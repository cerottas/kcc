{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "kcc-training.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kcc-training.labels" . | nindent 4 }}
{{- end }}
